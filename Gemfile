source "https://rubygems.org"

# This mirrors the GitHub Pages build environment, so a local preview
# matches what GitHub will publish. Only needed if you want to run the
# site locally with `bundle exec jekyll serve`.
gem "github-pages", group: :jekyll_plugins

# Windows / newer Ruby compatibility
gem "webrick", "~> 1.8"
